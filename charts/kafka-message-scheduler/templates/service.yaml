apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-message-scheduler.fullname" . }}
  labels:
    {{- include "kafka-message-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-message-scheduler.selectorLabels" . | nindent 4 }}
