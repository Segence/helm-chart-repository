apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-message-scheduler.fullname" . }}-test-connection"
  labels:
    {{- include "kafka-message-scheduler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafka-message-scheduler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
